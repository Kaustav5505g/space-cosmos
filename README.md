# Space Cosmos

An immersive space exploration page with an ARIA AI assistant.

## Local development

Install the dependencies and run the Vercel development server:

```bash
npm install
vercel dev
```

Set `OPENAI_API_KEY` in a local `.env` file before using the assistant. Keep that file out of Git.
